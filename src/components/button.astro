---
const { icon, href, size, ...attrs } = Astro.props;
---

<button
  type="button"
  class={`flex justify-center items-center rounded-full border-2 border-[--line] text-[--white] px-4 py-2 hover:bg-[--line] transition-all duration-300 ease-in-out active:translate-y-1 ${size}`}
  onclick={href ? `window.open('${href}', '_blank', 'noopener,noreferrer')` : null}
  {...attrs}
>
  <slot />
</button>